# Join colors using commas

colors = ["red", "green", "blue"]
csv_data = ",".join(colors)
print(csv_data)

# This will Add a space after each comma for easier reading.

comma_list = ", ".join(colors)
print(comma_list)
